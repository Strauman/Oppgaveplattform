%!TEX root = ../main.tex
%%% Definitions and stuff
\def\versionText{}
\def\setNo{}
\def\setNos{}
\def\cycleNos{}
\def\courseCode{REA-0100}
\def\courseTitle{Read the README.pdf}
\def\exerciseSetName{Problem Set}
\IfLanguageName{norsk}{\def\exerciseSetName{Oppgavesett}}{}
\newif\ifBuildIntent
\newif\ifBuildOverview
\newif\ifBuildReadme
\newif\ifHideFootLine

\newif\ifBlankBeforeNew
\newif\ifBibliography


\let\BlankBeforeNew = \BlankBeforeNewtrue
\let\BuildOverview = \BuildOverviewtrue
\let\BuildReadme = \BuildReadmetrue
\let\HideFootLine = \HideFootLinetrue
\newcommand{\Bibliografi}[1]{
\Bibliographytrue
\def\bibliographyFile{#1}
}
\def\frontpage{problemset}
\newcommand{\Forside}[1]{\def\frontpage{#1}}
\newcommand{\Fagkode}[1]{\def\courseCode{#1}}
\newcommand{\Fagtittel}[1]{\def\courseTitle{#1}}
\newcommand{\Oppgavenavn}[1]{\def\exerciseSetName{#1}}
\newcommand{\Sets}[1]{\def\setNos{#1}}
\newcommand{\Cycles}[1]{\def\cycleNos{#1}}

%% Headers and footers
\pagestyle{fancy}
\fancyhf{}

\chead{\courseCode\ \courseTitle}
\newif\ifCustomBuild
\CustomBuildtrue
\let\buildAll = \CustomBuildfalse


\rfoot{Page \thepage\ of \pageref{LastPage}}
\IfLanguageName{norsk}{\rfoot{Side \thepage\ av \pageref{LastPage}}}{}
\cfoot{}
\AtBeginDocument{
\ifHideFootLine
\else
  \renewcommand{\footrulewidth}{0.4pt}
\fi
%%% Dynamic header and footer based on ifs in main.tex
\ifDisplaySolutions
  \ifDisplayDirections
    %% If display solutions AND direction
    \def\versionText{Solution and discussion}
    \IfLanguageName{norsk}{\def\versionText{Løsnings- og diskusjonsforslag}}{}
    \else
    %% If (display solutions) AND (NOT display direction)
    \def\versionText{Solution}
    \IfLanguageName{norsk}{\def\versionText{Løsningsforslag}}{}
  \fi
\else
  \ifDisplayDirections
    %if (display direction) AND (NOT display solution)
    \def\versionText{Discussion}
    \IfLanguageName{norsk}{\def\versionText{Diskusjonsforslag}}{}
  \else
    %If (NOT display direction) AND (NOT display solution)
    \def\versionText{}
  \fi
\fi
\rhead\versionText
\ifDisplayIdeas
  \lfoot{\color{red}Oppgaveideer er inkludert}
  \IfLanguageName{norsk}{\lfoot{\color{red}Oppgaveideer er inkludert}}{}
\fi
\lhead{\exerciseSetName\ \setNo}
\lfoot{\currentSubject}
}

%%% Front page %%%
\setlength{\parindent}{0pt}

\betterif\ifDisplayTitle
\let\DisplayTitle = \DisplayTitletrue
\let\HideTitle = \DisplayTitlefalse

\geometry{paper=a4paper, bottom=3cm, top=3cm, footnotesep=3cm}
